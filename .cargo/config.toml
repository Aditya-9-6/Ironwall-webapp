[build]
# Force single job for resource-constrained cloud builds (Render Free Tier)
jobs = 1


# sccache speeds up rebuilds — install with: cargo install sccache
# rustc-wrapper = "sccache"

[net]
retry = 3
